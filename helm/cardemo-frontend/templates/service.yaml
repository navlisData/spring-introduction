apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-springapp
  namespace: ns-car-springdemo
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-springapp
  type: NodePort